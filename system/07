#Figure out the PID of the SSHD service

#sshd -- OpenSSH SSH daemon
#PID -- process ID of this sshd
#provide secure encrypted communications between two untrusted hosts over an insecure network
#sshd will typically write a PID file; by default this is at /var/run/sshd.pid

cat /var/run/sshd.pid